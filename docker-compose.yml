---
services:
  flask-app:
    image: ${DOCKER_IMAGE:-vannabothcd/flask-app:latest}
    container_name: flask-app
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    restart: unless-stopped
